import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractGridColumnRenderer } from './abstract-grid-column-renderer.js';
class GridColumnBodyRendererDirective extends AbstractGridColumnRenderer {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.renderer = (root, column, model) => {
            if (model) {
                render(renderer.call(options.host, model.item, model, column), root, options);
            }
        };
    }
}
const rendererDirective = directive(GridColumnBodyRendererDirective);
export const columnBodyRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=grid-column-body-renderer.js.map