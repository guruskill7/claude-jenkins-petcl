import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractRendererDirective } from './abstract-renderer.js';
class NotificationRendererDirective extends AbstractRendererDirective {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.renderer = (root, notification) => {
            render(renderer.call(options.host, notification), root, options);
        };
    }
    /**
     * Run renderer callback on the element.
     */
    runRenderer(element) {
        element.requestContentUpdate();
    }
}
const rendererDirective = directive(NotificationRendererDirective);
export const notificationRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=notification-renderer.js.map