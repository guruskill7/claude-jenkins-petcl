import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractRendererDirective } from './abstract-renderer.js';
class ContextMenuRendererDirective extends AbstractRendererDirective {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.renderer = (root, menu, context) => {
            render(renderer.call(options.host, context, menu), root, options);
        };
    }
    /**
     * Run renderer callback on the element.
     */
    runRenderer(element) {
        element.requestContentUpdate();
    }
}
const rendererDirective = directive(ContextMenuRendererDirective);
export const contextMenuRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=context-menu-renderer.js.map