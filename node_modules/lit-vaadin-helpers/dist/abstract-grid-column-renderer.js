import { AbstractRendererDirective } from './abstract-renderer.js';
import { debounce } from './utils.js';
export class AbstractGridColumnRenderer extends AbstractRendererDirective {
    /**
     * Run renderer callback on the element.
     */
    runRenderer(element) {
        const grid = element._grid;
        if (grid) {
            // Only call grid.requestContentUpdate() once per property change
            // in case if that property is used by several column renderers.
            debounce(grid, () => {
                grid.requestContentUpdate();
            });
        }
    }
}
//# sourceMappingURL=abstract-grid-column-renderer.js.map