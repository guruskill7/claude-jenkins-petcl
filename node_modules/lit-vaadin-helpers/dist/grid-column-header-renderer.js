import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractGridColumnRenderer } from './abstract-grid-column-renderer.js';
class GridColumnHeaderRendererDirective extends AbstractGridColumnRenderer {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.headerRenderer = (root, column) => {
            render(renderer.call(options.host, column), root, options);
        };
    }
}
const rendererDirective = directive(GridColumnHeaderRendererDirective);
export const columnHeaderRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=grid-column-header-renderer.js.map