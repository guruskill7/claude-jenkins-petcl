import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractRendererDirective } from './abstract-renderer.js';
class SelectRendererDirective extends AbstractRendererDirective {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.renderer = (root, select) => {
            render(renderer.call(options.host, select), root, options);
        };
    }
    /**
     * Run renderer callback on the element.
     */
    runRenderer(element) {
        element.requestContentUpdate();
    }
}
const rendererDirective = directive(SelectRendererDirective);
export const selectRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=select-renderer.js.map