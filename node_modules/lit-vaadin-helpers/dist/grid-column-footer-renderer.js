import { render } from 'lit';
import { directive } from 'lit/directive.js';
import { AbstractGridColumnRenderer } from './abstract-grid-column-renderer.js';
class GridColumnFooterRendererDirective extends AbstractGridColumnRenderer {
    /**
     * Set renderer callback to the element.
     */
    addRenderer(element, renderer, options) {
        element.footerRenderer = (root, column) => {
            render(renderer.call(options.host, column), root, options);
        };
    }
}
const rendererDirective = directive(GridColumnFooterRendererDirective);
export const columnFooterRenderer = (renderer, value) => rendererDirective(renderer, value);
//# sourceMappingURL=grid-column-footer-renderer.js.map